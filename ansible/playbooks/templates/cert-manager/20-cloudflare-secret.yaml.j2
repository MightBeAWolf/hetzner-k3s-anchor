---
# Cloudflare API token for DNS-01 challenge
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
stringData:
  api-token: "{{ lookup('env', 'TF_VAR_cloudflare_api_token') }}"
